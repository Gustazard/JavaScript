// (const) usado para declarar uma váriavel de valor fixo, ou seja que nunca será alterada.

//(let) é usado para declarar uma váriavel de valor NÃO fixo, ou seja, podemos a qualquer momento alterar o valor dela.

let mensagemTela = ("Este texto é um exemplo para podermos praticar o exercicio");

//console.log(mensagemTela);

mensagemTela = ("Aqui estou alteradno o valor da minha variavel tipo LET");

//console.log(mensagemTela);

const mensagemExemplo = ("Esta mensagem é um teste");
//console.log (mensagemExemplo);

mensagemExemplo = ("Estou alterando o texto para testes")
console.log (mensagemExemplo);